# VOLK-ML

VOLK-ML (Vivid Online Learning Kit for Machine Learning) is a client-side educational ML pipeline builder with bilingual English/Chinese UI and a Parallel language mode.

## Run

```bash
npm run dev
```

Then open <http://localhost:5173>.

## Build

```bash
npm run build
```

The static GitHub Pages-ready output is written to `dist/`.
